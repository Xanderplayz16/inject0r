{
  "name": "injectorbackend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "unbuild": "rm -rf node_modules || rd /s /q node_modules && rm package-lock.json || echo '--------------.Done.-----------------'",
    "build": "npm run unbuild || bun run unbuild && npm install || bun install",
    "start": "bun run index.js || node index.js"
  },
  "keywords": [],
  "author": "paragram",
  "license": "ISC",
  "_comment": "lol no freaking way the server is under isc",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "rimraf": "^3.0.2"
  }
}
